<UserControl x:Class="ToolCore.Logging.Wpf.Controls.LiveLogViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="220"
             d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding LogType}" Value="WAR">
                    <Setter Property="Background" Value="#FFF8E7B2" />
                </DataTrigger>
                <DataTrigger Binding="{Binding LogType}" Value="ERR">
                    <Setter Property="Background" Value="#FFF4C7C3" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border BorderBrush="#D0D0D0"
            BorderThickness="1"
            Background="White">

        <DockPanel>

            <Grid DockPanel.Dock="Top" Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Activity Log"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold" />

                <Button Grid.Column="1"
                        Width="80"
                        Height="28"
                        Margin="8,0,0,0"
                        Content="Clear" />
            </Grid>

            <DataGrid x:Name="LogGrid"
                      Margin="8,0,8,8"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="" Width="30">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="ℹ" />
                                            <Setter Property="Foreground" Value="#2F6FED" />

                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding LogType}" Value="WAR">
                                                    <Setter Property="Text" Value="⚠" />
                                                    <Setter Property="Foreground" Value="#C98A00" />
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding LogType}" Value="ERR">
                                                    <Setter Property="Text" Value="✖" />
                                                    <Setter Property="Foreground" Value="#C62828" />
                                                </DataTrigger>

                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Time"
                                        Width="110"
                                        Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" />
                    <DataGridTextColumn Header="Message"
                                        Width="*"
                                        Binding="{Binding Message}" />
                </DataGrid.Columns>
            </DataGrid>

        </DockPanel>
    </Border>
</UserControl>